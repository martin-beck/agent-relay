[project]
name = "agent-relay-quality"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
quality = ["pre-commit==4.6.2"]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["B", "C4", "E4", "E7", "E9", "F", "I", "PIE", "RUF", "SIM", "UP"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
python_version = "3.12"
scripts_are_modules = true
show_error_codes = true
strict = true
warn_unreachable = true
